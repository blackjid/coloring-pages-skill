{
  "name": "coloring-pages-skill",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "tsc && concurrently \"npm run tsc:w\"",
    "tsc": "tsc",
    "tsc:w": "tsc -w",
    "start": "tsc && cd built && node-lambda run --eventFile ../event.json --contextFile ../context.json --configFile ../.env.local"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "alexa-sdk": "^1.0.6",
    "googleapis": "^14.1.0",
    "node-lambda": "^0.8.11"
  },
  "devDependencies": {
    "@types/alexa-sdk": "^1.0.3",
    "@types/node": "< 6.*",
    "concurrently": "^3.1.0",
    "google-api-nodejs-tsd": "procrastinatos/google-api-nodejs-tsd",
    "typescript": "^2.0.7"
  }
}
